import styled from "styled-components";

const Container = styled.div`
  max-width: 1440px;
  padding: var(--padding);
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 20px;
`;

export { Container };
